<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.OtoDienSaleShop" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- 🔹 Màu thương hiệu (giữ tông xanh điện nhưng dịu hơn) -->
        <item name="colorPrimary">@color/deep_blue</item>
        <item name="colorPrimaryVariant">@color/electric_blue</item>
        <item name="colorOnPrimary">@color/text_light</item>

        <!-- 🔸 Màu phụ -->
        <item name="colorSecondary">@color/teal_500</item>
        <item name="colorOnSecondary">@color/text_light</item>

        <!-- 🌌 Nền tối -->
        <item name="android:windowBackground">@color/dark_bg</item>
        <item name="android:textColor">@color/text_light</item>
        <item name="android:statusBarColor">@color/dark_surface</item>

        <!-- 🧩 Font -->
        <item name="android:fontFamily">@font/poppins_regular</item>
    </style>

</resources>
